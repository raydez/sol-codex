# Contributing

1. Use Node.js 22+.
2. Run `npm install` and `npm test`.
3. Keep mechanism core code independent from Codex hook JSON where possible.
4. Never trade correctness or required verification for token savings.
5. New optimization behavior must have a feature flag and a native-behavior fallback.
6. New reducers must preserve exact source evidence and include verification tests.
